{
  "name": "solaris-energy-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Pixel-faithful local clone of the Solaris Energy landing page.",
  "scripts": {
    "dev": "npx -y http-server . -p 3000 -a 127.0.0.1 -c-1 --cors",
    "start": "npx -y http-server . -p 3000 -a 127.0.0.1 -c-1 --cors"
  }
}
